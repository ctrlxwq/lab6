{
  "name": "ci-pipeline-lab",
  "version": "1.0.0",
  "main": "src/calc.js",
  "scripts": {
    "lint": "echo 'Lint passed!'",
    "test": "jest --coverage --ci --reporters=default --reporters=jest-junit",
    "build": "mkdir -p dist && cp -r src dist/"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "jest-junit": "^16.0.0"
  }
}
